const initialState = {
  logos: [
    "mcm.svg",
    "walmart.svg",
    "hp.svg",
    "nyulangone.svg",
    "nbc.svg",
    "google.svg"
  ]
};

export default function reducer(state=initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

