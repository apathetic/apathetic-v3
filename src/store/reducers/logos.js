const initialState = {
  logos: [
    "mcm.svg",
    "google.svg",
    "hp.svg",
    "mcm.svg",
    "walmart.svg"
  ]
}

export default function reducer(state=initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

