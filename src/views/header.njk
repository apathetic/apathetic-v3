
    <header class="relative text-gray-300 px-8 py-4">
      <div class="weshatch leading-none cursor-pointer">
        <h1 class="font-bold">Wes Hatch</h1>
        <h2 class="text-sm">front-end developer</h2>
      </div>

      <nav class="inline-block">
        <ul class="flex">
          <li class="text-sm ml-4 {{ 'active' if page.url === '/' }}">
            <a href="/">about</a>
          </li>
          <li class="text-sm ml-4 {{ 'active' if ('posts' in page.url or 'blog' in page.url) }}">
            <a href="/blog">blog</a>
          </li>
          <li class="text-sm ml-4 {{ 'active' if page.url === '/demos/' }}">
            <a href="/demos">demos</a>
          </li>
        </ul>
      </nav>

      {%- if page.url.includes('/content') %}
      <a class="absolute left-4 bottom-4 hidden lg:inline-block" href="/blog">⇦ back</a>
      {% endif %}
    </header>
